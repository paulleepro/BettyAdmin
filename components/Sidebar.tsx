import styled from "styled-components";

const SidebarContainer = styled.div`
  margin-right: 1rem;
  padding: 2rem 0;
  width: 200px;
`;

export const Sidebar = () => <SidebarContainer>Dashboard</SidebarContainer>;
